---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: schemaregistry
  namespace: ${NAMESPACE}
spec:
  parentRefs:
  - name: envoy-gateway
    namespace: ${ENVOY_GATEWAY_NAMESPACE}
  hostnames:
  - "schemaregistry.${BASE_DOMAIN}"  # The SNI hostname to look for
  rules:
  - backendRefs:
    - name: schemaregistry-0-internal
      port: 8081  # The port on your pod where TLS is terminated
